import React from 'react';
import { expect, it, describe } from 'vitest'
import { render } from '@testing-library/react'
import { {{ pascalCase func }} } from '.'


describe('{{ pascalCase func }}', async () => {
  it('should render', async () => {
    const { getByTestId } = render(<{{ pascalCase func }} />)
    expect(getByTestId('{{ pascalCase func }}')).toBeDefined()
  })
})
